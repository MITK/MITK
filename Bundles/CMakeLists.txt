#SET(OPENCHERRY_SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../Utilities/openCherry)
#SET(MITK_CORE_BUNDLES ${CMAKE_CURRENT_SOURCE_DIR}/org.mitk.core)

#INCLUDE(${OPENCHERRY_SOURCE_DIR}/CMake/FindPoco.cmake)

IF(BUILD_OPENCHERRY)

  #FIND_POCO()
  
  INCLUDE_DIRECTORIES(${${KITNAME}_INCLUDE_DIRS})

  COLLECT_PLUGINS(${LIBRARY_OUTPUT_PATH}/Bundles ON OFF)
   
ENDIF(BUILD_OPENCHERRY)