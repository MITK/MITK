INCLUDE(${OPENCHERRY_SOURCE_DIR}/CMake/PluginHelpers.cmake)
    
# bad hack to include org.mitk.core in include paths
SET(PLUGIN_DIR ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../org.mitk.core)
    
INCLUDE_DIRECTORIES(${PLUGIN_DIR} ${OPENCHERRY_SOURCE_DIR} ${POCO_INCLUDE_DIRS})

INCLUDE_DIRECTORIES(${Q${KITNAME}_INCLUDE_DIRS})
ADD_DEFINITIONS(${QT_DEFINITIONS})

COLLECT_PLUGINS(OFF ON)