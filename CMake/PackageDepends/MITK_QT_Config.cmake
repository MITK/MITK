message(WARNING "You used a deprecated keyword PACKAGE_DEPENDS QT.\nPlease switch to QT4 or QT5 or investigate more to support both versions.")
include (MITK_QT_Config)

#find_package(Qt4 REQUIRED)
#include(${QT_USE_FILE})
#list(APPEND ALL_INCLUDE_DIRECTORIES ${QT_INCLUDE_DIRS})
#list(APPEND ALL_LIBRARIES ${QT_LIBRARIES})

