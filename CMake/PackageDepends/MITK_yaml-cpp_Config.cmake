message("Inluding yaml-cpp libs: ${YAML_CPP_LIBRARIES}, includes: ${YAML_CPP_INCLUDE_DIRS}.")
list(APPEND ALL_LIBRARIES ${YAML_CPP_LIBRARIES})
if(YAML_CPP_INCLUDE_DIRS)
  list(APPEND ALL_INCLUDE_DIRECTORIES ${YAML_CPP_INCLUDE_DIRS})
endif()
