IF (APPLE)
  MESSAGE(STATUS "Module GPGPU is not ported to MacOS yet")
ELSE(APPLE)

MITK_CREATE_MODULE(mitkGPGPU
#  INCLUDE_DIRS .
  DEPENDS Mitk glew
  PACKAGE_DEPENDS QT
  QT_MODULE
)

ENDIF(APPLE)

