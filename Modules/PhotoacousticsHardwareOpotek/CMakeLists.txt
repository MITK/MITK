IF(WIN32)

OPTION(MITK_USE_OPOTEK_SDK "Enable support for OPOTEK Lasers" OFF)

IF(MITK_USE_OPOTEK_SDK)

SET(MITK_OPOTEK_SDK_PATH "" CACHE PATH "Path to OPOTEK SDK header files.")

MITK_CREATE_MODULE(
  SUBPROJECTS
  DEPENDS MitkPhotoacoustics
  INCLUDE_DIRS PUBLIC "${MITK_OPOTEK_SDK_PATH}"
  INTERNAL_INCLUDE_DIRS ${INCLUDE_DIRS_INTERNAL}
  ADDITIONAL_LIBS "${MITK_OPOTEK_SDK_PATH}/OPOTEK.lib"
  # AUTOLOAD_WITH MitkPhotoacoustics
)

ENDIF(MITK_USE_OPOTEK_SDK)

ENDIF(WIN32)