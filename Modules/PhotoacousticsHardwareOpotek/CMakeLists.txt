IF(WIN32)

OPTION(MITK_USE_OPOTEK_HARDWARE "Enable support for OPOTEK Lasers" OFF)

IF(MITK_USE_OPOTEK_HARDWARE)

SET(MITK_OPOTEK_SDK_PATH "" CACHE PATH "Path to OPOTEK SDK header files.")

SET(MITK_GALIL_API_PATH "" CACHE PATH "Path to Galil API header files.")

MITK_CREATE_MODULE(
  SUBPROJECTS
  DEPENDS MitkPhotoacoustics
  INCLUDE_DIRS PUBLIC "${MITK_OPOTEK_SDK_PATH}" "${MITK_GALIL_API_PATH}/include"
  INTERNAL_INCLUDE_DIRS ${INCLUDE_DIRS_INTERNAL}
  ADDITIONAL_LIBS "${MITK_OPOTEK_SDK_PATH}/OPOTEK.lib" "${MITK_GALIL_API_PATH}/lib/dynamic/x64/gclib.lib" "${MITK_GALIL_API_PATH}/lib/dynamic/x64/gclibo.lib" tinyxml
  PACKAGE_DEPENDS tinyxml
)

ENDIF(MITK_USE_OPOTEK_HARDWARE)

ENDIF(WIN32)