FOREACH(dir  .  QmitkApplicationBase QmitkPropertyObservers)
  SET(MODULE_INCLUDE_DIRS ${MODULE_INCLUDE_DIRS} ${CMAKE_CURRENT_SOURCE_DIR}/${dir})
ENDFOREACH(dir in ${CORE_DIRECTORIES})
LIST(APPEND MODULE_INCLUDE_DIRS ${CMAKE_CURRENT_BINARY_DIR})
SET(MODULE_NAME QmitkExt)
CONFIGURE_FILE("${CMAKE_CURRENT_SOURCE_DIR}/moduleIncludeConf.cmake.in" "${PROJECT_BINARY_DIR}/${MODULE_NAME}Config.cmake" @ONLY)


SET(QT_USE_QT3SUPPORT 1)

INCLUDE(${PROJECT_BINARY_DIR}/mitkExtConfig.cmake) 

SET(Q${KITNAME}_INCLUDE_DIRS 
      ${${KITNAME}_INCLUDE_DIRS} 
      ${Q${KITNAME}_INCLUDE_DIRS} 
      ${CMAKE_CURRENT_SOURCE_DIR} 
      ${PROJECT_BINARY_DIR}/Modules/QmitkExt
      ${QT_INCLUDE_DIR}
   )

FOREACH(dir QmitkApplicationBase QmitkPropertyObservers) 
  SET(Q${KITNAME}_INCLUDE_DIRS ${Q${KITNAME}_INCLUDE_DIRS} ${CMAKE_CURRENT_SOURCE_DIR}/${dir})
ENDFOREACH(dir)

SET(Q${KITNAME}_INCLUDE_DIRS ${Q${KITNAME}_INCLUDE_DIRS} ${mitkExt_INCLUDE_DIRS})

CREATE_Q4MITK(SHARED)
TARGET_LINK_LIBRARIES(QmitkExt Qmitk qwt mitkCoreExt)
