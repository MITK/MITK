SET(QT_USE_QT3SUPPORT 1)

INCLUDE(${PROJECT_BINARY_DIR}/mitkExtConfig.cmake) 

SET(Q${KITNAME}_INCLUDE_DIRS 
      ${${KITNAME}_INCLUDE_DIRS} 
      ${Q${KITNAME}_INCLUDE_DIRS} 
      ${CMAKE_CURRENT_SOURCE_DIR} 
      ${PROJECT_BINARY_DIR}/Modules/QmitkExt
      ${QT_INCLUDE_DIR}
   )

FOREACH(dir QmitkFunctionalityBase QmitkPropertyObservers) 
  SET(Q${KITNAME}_INCLUDE_DIRS ${Q${KITNAME}_INCLUDE_DIRS} ${CMAKE_CURRENT_SOURCE_DIR}/${dir})
ENDFOREACH(dir)

SET(Q${KITNAME}_INCLUDE_DIRS ${Q${KITNAME}_INCLUDE_DIRS} ${mitkExt_INCLUDE_DIRS})

CREATE_Q4MITK(SHARED)
TARGET_LINK_LIBRARIES(QmitkExt qwt)
