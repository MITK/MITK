IF(WIN32)

OPTION(MITK_USE_US_FRAUNHOFER_SDK "Enable support for Fraunhofer api devices" OFF)

IF(MITK_USE_US_FRAUNHOFER_SDK)

SET(MITK_US_FRAUNHOFER_SDK_PATH "" CACHE PATH "Path to Fraunhofer SDK header files.")

MITK_CREATE_MODULE(
  SUBPROJECTS
  DEPENDS MitkUS
  INCLUDE_DIRS "${MITK_US_FRAUNHOFER_SDK_PATH}"
  INTERNAL_INCLUDE_DIRS ${INCLUDE_DIRS_INTERNAL}
  AUTOLOAD_WITH MitkUS
)

ENDIF(MITK_USE_US_FRAUNHOFER_SDK)

ENDIF(WIN32)
