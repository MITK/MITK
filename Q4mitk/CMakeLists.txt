# stub for Qt4 
#
ADD_DEFINITIONS(${QT_DEFINITIONS})
INCLUDE_DIRECTORIES(${QT_INCLUDES})

# something like this should work
# ADD_EXECUTABLE(Step1 Step1.cpp Q4mitkRenderWindow.cpp)
# TARGET_LINK_LIBRARIES(Step1 ${MITK_LIBRARIES})

