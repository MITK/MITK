if(UNIX AND DESIRED_QT_VERSION MATCHES 4)
  set( CMAKE_CXX_FLAGS  "-fPIC ${CMAKE_CXX_FLAGS}" )
endif()

mitk_create_module(
  PACKAGE_DEPENDS
    PUBLIC Qt5|Widgets
    PRIVATE Qt5|Network
  FORCE_STATIC
  NO_INIT
)
